PROCEDURE max(x, y);
    BEGIN
        max := x;
        EXIT;
        IF y > x THEN max := y;
    END;

BEGIN
    WRITELN(max(3, 5));
    
    FOR i := 1 TO 10 DO
        IF (i mod 2) = 0 THEN
            BREAK;
        ELSE
            WRITELN(i + 1000);
END;
.